/**
 * Placeholder shared type — proves the `@scrum-poker/types` package resolves,
 * extends `@scrum-poker/config`'s strict tsconfig, and type-checks under the
 * workspace tooling. Real domain types (Session / Estimation / Vote) land in
 * issue #7.
 */
export interface Placeholder {
  readonly ok: boolean;
}
