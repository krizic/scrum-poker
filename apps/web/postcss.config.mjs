// PostCSS config for Tailwind CSS v4. The single `@tailwindcss/postcss` plugin
// replaces the v3 `tailwindcss` + `autoprefixer` pair.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
